/**
 * Configuration for Posters
 */

config.dataFileUrl      = 'posters.csv';
config.visibleColumns   = ['Authors', 'Affiliation', 'Title', 'Type'];
config.requiredFields   = ['Url'];
config.columnWidthOverrides = {
    'Type': 0.25,
};
